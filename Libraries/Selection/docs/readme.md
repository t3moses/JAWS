Sort functions are used to order boats and crews according to rank.
Boats and crews with identical ranks are ordered randomly.
Sort() takes a list of objects that have a rank property and orders them by increasing rank.
Rank is an array of numbers.  Each position represents a component of rank in decreasing order of significane.  The value of each component reflects the strength of that component.